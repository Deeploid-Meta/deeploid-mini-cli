TOOL = config["tool"]
FORWARD_READS = config["forward_reads"]
REVERSE_READS = config["reverse_reads"]
OUTDIR = config["outdir"]
DATABASE = config["database"]
THREADS = ["threads"]
WORKING_DIR = config["working_dir"]
TAXANOMY = "/Users/d.voronkina/Desktop/Deeploid/snake/deeploid-mini-cli/databases/GG/85_otu_taxonomy.txt"

rule all:
    input: OUTDIR

rule envs:
    params:
        qiime2 = "../envs/qiime2_L.yaml"
        # dada2 = "envs/dada2.yaml"
      
envs = rules.envs.params
include: "./rules/qiime2.smk"